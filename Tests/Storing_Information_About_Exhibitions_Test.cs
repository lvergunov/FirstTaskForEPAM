using Microsoft.VisualStudio.TestTools.UnitTesting;
using FirstTaskForEPAM;

namespace Tests
{
    /// <summary>
    /// A class that tests whether an object keeps a history of its exhibitions after it has been moved
    /// </summary>
    [TestClass]
    public class Storing_Information_About_Exhibitions_Test
    {
        /// <summary>
        /// A method that tests whether an object keeps a history of its exhibitions after it has been moved
        /// </summary>
    }
}
